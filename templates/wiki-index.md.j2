---
type: index
section: {{ section }}
updated: {{ date }}
---

# {{ title }}

{% if section == "master" %}
> Paper Distill knowledge base — auto-maintained by LLM compilation.
> AI agents: use `query_vault` MCP tool instead of reading this file.

## Stats

- Papers ingested: `= length(filter(pages("Paper Distill/raw"), (p) => p.citekey))`
- Papers compiled: `= length(filter(pages("Paper Distill/wiki/papers"), (p) => p.citekey))`
- Concepts: `= length(pages("Paper Distill/wiki/concepts"))`
- Methods: `= length(pages("Paper Distill/wiki/methods"))`
- Topics: `= length(pages("Paper Distill/wiki/topics"))`

## Recent Papers

```dataview
TABLE year, join(topics, ", ") as Topics
FROM "Paper Distill/raw"
WHERE citekey
SORT ingested DESC
LIMIT 10
```

## Topics

```dataview
LIST
FROM "Paper Distill/wiki/topics"
SORT file.name ASC
```
{% endif %}

{% if section == "inbox" %}
> Candidate papers awaiting human approval. AI agents: use `query_vault(section="inbox")`.

```dataview
TABLE status, venue_normalized as Venue, score_total as Score, join(matched_topics, ", ") as Topics
FROM "Paper Distill/inbox"
WHERE paper_id
SORT retrieved_at DESC
```
{% endif %}

{% if section == "raw" %}
> Chronological list of all ingested papers. AI agents: use `query_vault(section="raw")`.

## All Raw Papers

```dataview
TABLE year, join(topics, ", ") as Topics, compiled as Compiled
FROM "Paper Distill/raw"
WHERE citekey
SORT ingested DESC
```

## Uncompiled

```dataview
LIST
FROM "Paper Distill/raw"
WHERE compiled = false
SORT ingested DESC
```
{% endif %}

{% if section == "papers" %}
> Deep compiled reading notes. AI agents: use `query_vault(section="papers")`.

## All Papers

```dataview
TABLE year, join(topics, ", ") as Topics, quality_score as Quality, relevance_score as Relevance
FROM "Paper Distill/wiki/papers"
WHERE citekey
SORT year DESC
```

## By Topic

{% for topic_key, topic in topics.items() %}
### {{ topic.label }}

```dataview
TABLE year, authors[0] as "First Author"
FROM "Paper Distill/wiki/papers"
WHERE contains(topics, "{{ topic_key }}")
SORT year DESC
```

{% endfor %}
{% endif %}

{% if section == "concepts" %}
> Concept encyclopedia. AI agents: use `query_vault(section="concepts")`.

```dataview
TABLE paper_count as "Papers", join(topics, ", ") as Topics
FROM "Paper Distill/wiki/concepts"
SORT paper_count DESC
```
{% endif %}

{% if section == "methods" %}
> Method comparison articles. AI agents: use `query_vault(section="methods")`.

```dataview
TABLE join(topics, ", ") as Topics
FROM "Paper Distill/wiki/methods"
SORT file.name ASC
```
{% endif %}

{% if section == "topics" %}
> Research landscape articles. AI agents: use `query_vault(section="topics")`.

```dataview
LIST
FROM "Paper Distill/wiki/topics"
SORT file.name ASC
```
{% endif %}

# {{ title | default('Untitled Paper') }}

## Summary

{{ summary | default('No TL;DR available yet.') }}

## Abstract

{{ abstract | default('Abstract unavailable.') }}

## Why Recommended

{{ why_recommended | default('Recommended by the current ranking pipeline.') }}

## Score Breakdown

{% for key, value in score_breakdown.items() -%}
- {{ key }}: {{ value }}
{% endfor %}

## Venue / Author Signals

- Venue: {{ venue_raw | default('Unknown') }}
- Venue normalized: {{ venue_normalized | default('Unknown') }}
- Venue tier: {{ venue_tier | default('unknown') }}
- Venue source: {{ venue_source | default('unknown') }}
- Best topic: {{ best_topic | default('unknown') }}

## Links

- DOI: {{ doi | default('N/A') }}
- HTML: {{ canonical_html_url | default('N/A') }}
- PDF: {{ canonical_pdf_url | default('N/A') }}
- Item: {{ canonical_item_url | default('N/A') }}

## Human Decision

- Update `status` in frontmatter to `approved`, `rejected`, or `deferred`.
- Optionally write rationale in `decision_note`.
